# 15종 최종 family 결과 — **v3** (2026-08-24)

> **정본 테이블: `output_15sp_v2/summary_v3.tsv` — 20,133 families / 459,398 유전자.**
> v2의 "파편화 주석"이 이 판에서 **트리 검증된 병합**으로 확정됐다: 5,618개 클러스터 전수에
> 코돈 트리를 세워, 조각이 실제로 뒤섞인 경우만 병합했다. 남은 미결은 codeml 선택압 하나(맨 아래).
> v2 테이블(`summary.tsv`, 23,744)은 병합 전 상태로 보존된다.
>
> 🔴 **20,133은 상한이지 확정 수가 아니다 (2026-08-24 측정).** 트리를 본 family는
> **16,061/23,744 = 67.6%**뿐이고, **7,683개(32.4%)는 클러스터에도 트리에도 도달하지
> 못했다.** 원인은 증거가 아니라 임계값이다 — 간선 파일의 frac 최솟값이 정확히 **0.600**,
> 그 컷에 걸려 **8,902개 family가 나가는 간선 0**이다. 미판정 family는 작지도 않다(중앙값 15,
> 최대 1,278). 아래 "확정" 절은 **주어진 조각에 대한 증거이고, 테이블 3분의 1에 대해서는
> 침묵**이다. 자세한 진단과 복구 계획은 이 문서 끝의 "커버리지 구멍" 절.

## 런 정보

| | |
|---|---|
| SLURM | 6097190, **COMPLETED**, 1-12:43:29 |
| config | `config_15sp_v2.json` · 추정 종트리(135 loci, 전 내부노드 100/100) · Cgig=Helixer |
| 수렴 | R1 22,051 → R6–7 0 |
| 무결성 | 유실 0 · 중복 0 · rescue 청크 48/48 |

## 인용 가능 — 배치율

**comparable(≥100 aa + 완전 CDS)만 종간 비교에 쓴다.** headline: Mcry raw 10.8% vs baseline 14.9% **PASS**.

| 종 | comparable | raw | | 종 | comparable | raw |
|---|---|---|---|---|---|---|
| Ococ | **0.0%** | 0.0% | | Pole | 1.7% | 3.6% |
| Cjam | 0.3% | 1.6% | | Cgig | 1.7% | 5.9% |
| Ccac | 0.7% | 11.2% | | Sund | 1.9% | 3.0% |
| Obas | 0.8% | 2.8% | | Bvul | 2.7% | 5.3% |
| Ahyp | 0.9% | 4.0% | | Sof | 2.7% | 3.1% |
| Tfru | 1.0% | 12.8% | | **Mcry** | **2.8%** | 10.8% |
| Dcar | 1.6% | 2.5% | | Pami | 5.2% | 7.2% |
| Sole | 1.6% | 4.1% | | | | |

raw 상위 3종(Tfru·Ccac·Mcry)은 전부 주석 정책 산물. **"Mcry 문제"는 종결** — comparable에서 Bvul·Sof 동급.

## ✅ 확정 — rescue 층 (v1에서 잠정이었던 것)

게이트 보정 판정(#47): 프로파일 커버리지 게이트는 rescue 맥락에서 **판별력 0**
(comparable 거부 96% vs 깨진 모델 99%; 건강한 전장 멤버의 profile_cov 중앙값 0.22 — 프로파일
길이가 갭으로 부풀기 때문). `rescue_min_profile_coverage=0.0` 기본(off), 쿼리 게이트 유지(`91dc96c`).

```
최종 rescue: HIGH 8,159 / PROVISIONAL 1,139 / UNRESOLVED 1,083  ->  배정 9,298
             (옛 E-value 판정 10,381 대비: 게이트 탈락 ~1,083, family 변경 28)
배정 목록: ~/scratch/rescue_dom_15sp/final_assign.json
```

## ✅ 확정 — family 경계 (**트리에 도달한 67.6%에 한해** 검증 완료)

⚠️ 이 절의 모든 수치는 **트리 심판을 받은 16,061 family**에 대한 것이다. 나머지 7,683개는
판정을 받은 게 아니라 **심판대에 오르지 못했다**(위 헤더 및 아래 "커버리지 구멍" 절).

5,618개 파편화 클러스터 **전수**에 대해 정렬(MAFFT)→코돈 사영(pal2nal)→트리(FastTree)를 세우고,
`steps/cluster_validate.py`의 규칙으로 판정했다 (**5,618/5,618, 실패 0**):

- **INTERLEAVED** (조각 9,228개) — 트리가 조각을 뒤섞는다 = 계통 축 파편화 = 한 family.
  **병합군 2,410개**(family 6,021개)로 묶여 v3에 반영: **23,744 → 20,133 families (−3,611)**
- **MONOPHYLETIC** (조각 6,833개) — 자기 clade를 이룬다. 같은 family의 서브패밀리인지 이웃
  family인지 **위상만으로는 못 정하므로 병합하지 않고** 그대로 둔다 (PPC4가 그 사례 —
  PEPC 클러스터 안에서 단계통이고 5sp에서 별도 family였다)

무결성: 유전자 459,398 전량 보존 · 중복 0 · family 수 = 23,744 − Σ(병합군−1) 단언 통과.
`merged_from`/`cluster_id` 컬럼이 병합 provenance를 담는다.

**known-answer**: PEPC 계통 조각 3개(0000440+0013449+0019668)가 84유전자 한 family로 병합,
1E2(0008467)·PPC4(0009826)는 미판정 유지 — 파일럿과 전수 결과 일치.

PEPC 서브패밀리 정본은 여전히 `mM_repair/clan_corrected`(1E1 62 [95/91] · 1E2 15 [100/76],
둘 다 PROVISIONAL). **서브패밀리는 v3에서도 family 테이블이 아니라 clan 트리에서 나온다.**

## ✅ 확정 — 선택압 (교정 세트, HyPhy 삼중 2026-08-24)

교정 95-taxa / 1,428코돈, fgA 잎 10개 `{FG}` 태깅, HyPhy **2.5.100**:

| 검정 | 결과 | 폐기 세트 대비 |
|---|---|---|
| **RELAX** | LRT **51.89**, p **5.9e-13** — 선택 **이완** | 판정 재현 (구 LRT 68.3) |
| **MEME** | FDR 통과 **0** / 1,428 코돈 (명목 p≤0.05 7개) | 재현 |
| **aBSREL** | test 10가지 중 보정 유의 **1개 = Tfru 말단** (corrected p **1.15e-12**) | 재현 (구 p 6.0e-13) |

⚠️ **RELAX의 K는 인용하지 말 것** — 교정 세트에서 K가 1.25e-11로 **하한 경계에 붙었다**
(가지별 분포는 mean 1.22 / median 0.60). 판정(이완, 유의)만 인용한다. 폐기 세트의 0.636도
되살리지 않는다. test 가지 수가 19→10으로 다른 것은 태깅 정의 차이(잎만 vs clade 전체)이며,
더 좁은 test 집합에서도 같은 결론이 나온 것이므로 방향은 강화된다.

산출물: gpu `~/pepc_hyphy_corrected/{relax,meme,absrel}_corrected.json`

**codeml full-clan은 폐기했다.** 95-taxa 6체인(6097933-38)은 1일 16시간 동안 lnL 0/6으로
102-taxa 미수렴 전례를 그대로 따라가고 있었고, HyPhy-first 정책이 명시적으로 금지한 실행이다
(가설 판정은 위 삼중으로 완료). 2026-08-24 scancel. BEB 대조가 필요한 경우에 한해
fast-track 서브셋(`fast46`, 6099803)만 유지한다 — 정렬 컬럼 무손상이라 BEB 사이트 번호가
signal_windows 컬럼과 직접 호환된다.

## 산출물 인덱스 (pronghorn)

```
output_15sp_v2/summary_v3.tsv                  정본 family 테이블 (v3, 병합 적용)
output_15sp_v2/summary.tsv                     v2 (병합 전, 보존)
output_15sp_v2/cluster_verdicts.tsv            클러스터 트리 판정 전수 (5,618)
output_15sp_v2/merge_candidates_cov{0.5,0.2}.tsv   1층: 상호 쌍
output_15sp_v2/fragmentation_clusters.tsv      2층: 이웃 클러스터 (C0297 = PEPC)
output_15sp_v2/vote_edges.tsv                  간선 원자료
~/scratch/rescue_dom_15sp/final_assign.json    확정 rescue 배정
RETIRED_DO_NOT_USE/                            폐기물 (대장: retired_data.md)
```

---

## 🔴 커버리지 구멍 — v3가 테이블의 3분의 1을 보지 못했다 (2026-08-24)

### 측정

| | 수 | 비율 |
|---|---|---|
| 전체 family (`summary.tsv`) | 23,744 | |
| 나가는 간선(`from`) 보유 | 14,842 | 62.5% |
| **간선 0** | **8,902** | 37.5% |
| 간선 파일에 등장 (from∪to) = 트리 심판 대상 | 16,061 | 67.6% |
| **판정 자체를 못 받음** | **7,683** | **32.4%** |

미판정 family는 작아서 빠진 게 아니다: 크기 중앙값 **15**, p90 30, 최대 **1,278**.
라운드 분포는 후기 라운드에 쏠린다 — R1 70.6% 검토 vs **R2 30.8% · R3 20.8% · R4 21.9%**.
후기 라운드 family는 정의상 R1에서 떨어져 나온 조각이므로, **캠페인이 고치려던 대상이
사각지대에 몰려 있다.**

### 원인 — 증거가 아니라 임계값

`vote_edges.tsv`의 frac **최솟값이 정확히 0.600**이다. 파일이 기록하는 바닥이 곧 컷이라는
뜻이고, 그 아래는 전부 버려졌다. 그리고 커버리지 게이트가 하나 더 겹친다:
`MERGE_MIN_PROFILE_COV = 0.5`인데, `rescue_min_profile_coverage`가 이미 측정해 둔 값은
**전장 멤버의 profile_cov 중앙값 0.22**다 — 같은 게이트가 rescue에서 판별력 0으로
판정되어 기본 off 된 바로 그 게이트다. 0.6 상호성이 적용되기도 전에 정당한 표가 잘린다.
실측: 게이트를 0.5→0.2로 낮추기만 해도 후보가 3,614 → 4,394쌍(**+21.6%**)으로 는다.

### known-answer가 이 구멍 안에 있다

Mcry PEPC flagship(`Mcry_Mcr8G11630`)이 든 `R2_OG0000359`는 `R1_OG0019668`에 표를 던진다 —
**C0297, 다른 PEPC 조각들이 병합해 들어간 바로 그 클러스터의 구성원**이다. 그런데
frac이 **2/9 = 0.22**라 컷에 걸렸고, `vote_edges`·`merge_candidates`·`cluster_verdicts`
어디에도 없다. **"별개 family로 판정된" 것이 아니라 판정 기회가 없었다.**
그래서 v3에서도 두 flagship은 갈라져 있다 — Ococ은 `R1_OG0000440`(C0297, 84유전자),
Mcry는 `R2_OG0000359`(9유전자, 미판정).

### 착수한 조치

- `MERGE_MIN_PROFILE_COV` 모듈 상수 → **`config.merge_min_profile_cov`** (커밋 `6c9cab1`).
  15sp 런이 cov 0.2와 0.5 두 산출물을 냈는데 어느 것도 설정 해시에 묶이지 않았다 —
  0.2를 만들려면 소스를 고쳐야 했기 때문이다. 이제 매니페스트가 잡는다.
- **`vote_edges` / `fragmentation_clusters`가 저장소로 들어왔다** (같은 커밋). v3를 만든
  두 파일은 이 저장소가 쓴 적이 없었고 생성 스크립트도 스캔 산출물(`merge_scan/`의
  domtblout)도 남지 않아 **재현이 불가능했다.** 간선은 컷 없이 내보내고, 클러스터링은
  연결성분이며, `edges <= families` 불변식을 기록 시점에 검사한다.

### ✅ 표본 대조 완료 (2026-08-24, S2 6099928) — 재작성본은 원본과 동치다

family 200개 / 유전자 4,182개(전 멤버)를 **23,744 프로파일 전부**에 검색해
(hmmsearch **3.2.1** — 프로파일을 빌드한 그 버전, 파이프라인과 동일 플래그) 발행된
`vote_edges.tsv`와 대조했다. 이미 계산된 domtblout으로 커버리지 게이트를 스윕:

| cov | 간선 | **정확 일치** | 불일치 | 누락 |
|---|---|---|---|---|
| 0.0 | 343 | 141 | 58 | 1 |
| 0.1 | 341 | 152 | 47 | 1 |
| **0.2** | 353 | **191 / 200** | **9** | **0** |
| 0.3 | 362 | 129 | 68 | 3 |
| 0.5 | 373 | 84 | 97 | 19 |

**cov 0.2에서 뾰족한 단봉.** 확정된 사실 셋:

1. **재작성본은 사라진 원본과 동치다** — 올바른 파라미터에서 95.5% 재현, 누락 0.
2. **발행된 `vote_edges.tsv`는 cov 0.2로 만들어졌다** — 소스 상수는 0.5였는데.
   "소스를 고쳐야 만들 수 있는 산출물"이 실측으로 확인됐다.
3. **원본은 family당 최상위 1개 간선만 남겼다** (200 family → 200행). 재작성본은 전부
   내보내며(353개), shipped 행은 전부 재작성본의 최상위와 일치한다.

남은 9건은 원인이 알려져 있다 — 표본 스캔의 타깃 DB가 4,182서열이라 원본(459,398)보다
E-value가 약 110배 유리해 경계선 히트의 통과가 갈린다. 전체 재스캔에서는 사라진다.

**게이트는 표를 버리는 게 아니라 돌린다** (유전자 단위 진단):

```
Sole_Spov3_chr6.01383  최강 히트 R1_OG0001440 (44 bits) → cov 0.43 게이트 → 기권
Tfru_..._000477        최강 히트 R1_OG0001440 (60 bits) → cov 0.18 게이트 → 기권
Bvul_EL10Ac9g21841     → 대신 R1_OG0003962 (30 bits, cov 0.92) 에 투표
```

강한 히트가 잘리고 약하지만 커버리지 좋은 히트가 표를 가져간다. 침묵보다 나쁘다.

→ **`merge_min_profile_cov` 기본값 0.5 → 0.2** (근거 셋 수렴: 발행 산출물 0.2 · rescue
실측 중앙값 0.22 · 스윕 단봉 0.2). 회귀 테스트로 고정.

### 남은 것

1. ~~표본 대조~~ ✅ 위.
2. **전체 재스캔** (S2 **6099988**, 24×8코어): 459,398 멤버 × 23,744 프로파일, cov 0.2.
   임계값이 바뀌면 기존 간선도 바뀌므로 8,902개가 아니라 전량이다.
3. **확장 클러스터 트리 판정** — 검증된 레시피 재사용(`merge_validate/array.sbatch`,
   5,618 클러스터 실패 0). ⚠️ `fragment_verdict`도 리포에 호출부가 없다
   (`detect_merge_candidates`와 같은 병) — 캠페인 전체가 pronghorn 임시 스크립트에만
   있으므로 CLI로 들여와야 재사용 가능하다.

그때까지 **20,133은 상한**으로 인용한다.
