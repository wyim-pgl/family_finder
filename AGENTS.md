# AGENTS.md

**정본은 `CLAUDE.md`다.** 아키텍처·명령·설계 결정은 전부 거기 있고 여기 복제하지 않는다 —
두 문서가 갈라지는 것이 이 저장소가 이미 겪은 실패다(트래커 이슈 3개가 겹쳐 같은 내용을
반복 검증하게 만든 사례). 이 파일은 `CLAUDE.md`를 읽지 않는 에이전트를 위한 진입점이며,
아키텍처가 아니라 **작업 규칙**만 담는다.

## 먼저 읽을 것 (순서대로)

1. `CLAUDE.md` — 아키텍처, 명령, 설계 결정, 흔한 함정
2. `resume.md` §5 — **조용한 실패 카탈로그.** 디버깅을 시작하기 전에 여기부터 본다.
   증상/진짜 원인/판정법 3열로 30건 넘게 실측 기록돼 있다
3. `results_15sp.md` — 지금 인용 가능한 결과와 그 한계

## 이 저장소의 실패 유형은 하나다 — 조용한 실패

에러 없이 그럴듯한 틀린 결과. 아래는 전부 실측에서 나온 규칙이다.

### 유전자는 조용히 사라진다

- 유전자를 다루는 모든 경로에서 **`placed + unplaced == total`을 단언**할 것
- resume 경로가 특히 위험하다 — 라운드 산출물을 쓰는 순서 하나로 유전자가 증발한다 (#48)
- `codon_align`이 돌려주는 `removed_ids`는 호출자가 반드시 회수한다 (`CLAUDE.md`)
- 크기 게이트가 OG를 해체할 때 프루닝을 **통과한** 유전자까지 버리지 않는지 확인

### 실패는 실패로 보고할 것

- 예외를 삼키고 매니페스트를 `completed`로 끝내지 말 것 (#49).
  부분 결과가 완주와 구분되지 않으면 그 출력이 다음 resume의 기준점이 된다
- **SLURM exit code로 codeml 성공을 판정하지 말 것** — 결과를 다 쓴 뒤 exit 1 하는 경우가
  3회 재현됐다. `lnL` 줄과 BEB 블록 존재로 판정한다 (`resume.md` §5)
- 진행 상황도 마찬가지 — `rub` 파일이 비어 있는 것은 정체가 아니라 버퍼링일 수 있다.
  건강한 잡을 그 오판으로 죽인 적이 있다

### 무근 트리에 rooted 판정을 쓰지 말 것

FastTree 트리는 unrooted다. 무근 트리에서 *"leaf 집합 S가 clade다"* 의 정의는
**S를 나머지 전체와 분리하는 간선이 존재한다**이며, 구현은 자손 클레이드뿐 아니라
**여집합**도 후보로 봐야 한다. 자손 클레이드만 보면 위상이 같고 루트 표기만 다른 두 Newick이
서로 다른 판정을 낸다 (#51).

**midpoint rooting은 해법이 아니다** — 결정적일 뿐 여전히 루트 하나에 의존하고, 절단 모델
하나가 midpoint를 옮긴다. 이 데이터셋은 절단 모델이 많다는 것이 이미 실측돼 있다.

판정 로직을 만지면 **rerooting 불변성 테스트**를 같이 넣는다. 같은 위상, 다른 Newick 표기가
동일한 verdict를 내는지 단언할 것:

```
((a1,a2),((b1,c1),(b2,c2)));
(a1,(a2,((b1,c1),(b2,c2))));
```

### 🔴 트리 판정(`cluster_validate`)을 family 증거로 과신하지 말 것

이 규칙은 **주어진 조각에 대한 증거이고, 나머지에 대해서는 침묵**이다. 두 가지를 반드시
같이 읽는다 (`results_15sp.md`의 "커버리지 구멍" · "무근 트리 결함" 절):

- **커버리지**: 15sp 런에서 트리 심판을 받은 family는 **16,061/23,744 = 67.6%**뿐이다.
  나머지 **7,683개(32.4%)** 는 "별개로 판정된" 게 아니라 **심판대에 오르지 못했다** —
  간선 파일의 frac 최솟값이 정확히 0.600이고, 그 컷에 8,902 family가 걸렸다.
- **`MONOPHYLETIC`은 "별개 family"가 아니라 "미판정"이다.** 같은 family 안의 서브패밀리가
  정확히 이렇게 보인다. 이 판정이나 `cluster_verdicts.tsv` 부재를 근거로
  "이 조각들은 별개 family다"라고 쓰면 **틀린다.**

**PEPC가 그 반례다.** clan은 6조각 128유전자로 갈라져 있는데:

- Mcry flagship이 든 `R2_OG0000359`는 `R1_OG0019668`(= 다른 PEPC 조각들이 병합해 들어간
  C0297의 구성원)에 표를 던졌지만 **frac 2/9 = 0.22**로 컷에 걸려 **판정 자체가 없다**
- 파편화 축은 **계통이 아니라 대부분 서브패밀리**다 — 15종 전부가 3–5개 조각에 흩어져 있고,
  `R1_OG0009826`(PPC4)은 15종 × 17유전자, `R1_OG0008467`(1E2)은 12종 × 18유전자다.
  서브패밀리는 정의상 단계통이므로 **이 규칙은 원리적으로 병합할 수 없다** (6조각 중 2개만 병합)
- v3가 인용하던 "PEPC 3조각 → 84유전자 병합" known-answer는 **폐기됐다** (루트 표기 인공물, #51)

**"PEPC clan은 한 family"라는 판정은 위상이 아니라 계통 외 증거에서 나온다** — 구조
(ESMFold+foldseek 97/111 단일 클러스터), 외부 앵커(ATH PPC1–4 대비 전원 plant-type),
clan 트리(`clan_merge.tsv`: Ppc1·Ppc2 쌍 TOGETHER).

### 비교 지표는 교란을 먼저 의심할 것

- **종간 미배치율**: 프로테옴마다 주석 길이 하한이 다르다(Cgig MAKER 최단 151 aa vs
  Mcry 3 aa). `measure_v2.py --pep-dir`로 길이 층화하고 `comparable` 지표를 병기한다.
  원시율 단독 종간 비교 금지
- **서브패밀리 이름 support 1.00**: support는 *투표한* 가중치 기준이라 20멤버 중 2개만
  주석이면 1.00이 나온다. **`coverage` 컬럼**을 볼 것
- **주석 세대**: 인트론 이탈률 같은 지표는 서브패밀리가 아니라 주석 프로그램을 따라간다.
  통제 없이 쓰면 주석 차이가 생물학으로 읽힌다
- **도구 판정을 채택하기 전에 known-answer 게이트**를 통과시킬 것. ECForest가 이 게이트로
  기각됐다

### 검정 도구 우선순위

HyPhy-first. codeml과 HyPhy가 어긋나면 **HyPhy가 이긴다** — 근거와 사례는 `CLAUDE.md`의
Selection-test policy 절에 있다.

## 작업 규칙

- **테스트**: `python3 -m pytest tests/ -q` — conda 환경 불필요. ete4는 `sys.modules`로 stub,
  외부 도구는 monkeypatch한다 (`tests/test_size_gate.py` 패턴)
- **문제를 발견하면 즉시 GitHub 이슈로 기록**한다. 자신의 사양 결함도 포함.
  단 **이슈 생성·코멘트는 외부 발신이므로 매번 사용자 승인**을 받는다. 검색은 무제한
- **도구·설치·파이프라인 실패는 디버깅 전에 `gh search issues` 먼저.**
  `--state`를 주지 말 것(닫힌 스레드에 답이 있다)
- **커밋 저자는 Won Yim <wyim@unr.edu> 단독.** `Co-Authored-By`, 세션 URL, AI 생성 푸터를
  커밋 메시지·PR 본문·이슈 어디에도 넣지 않는다
- 클러스터 경로: `ssh pronghorn`, 실행 디렉터리 `~/scratch/bin/family_finder/`.
  ⚠️ `~/scratch`는 심볼릭 링크라 `find ~/scratch ...`가 **아무것도 못 찾고 exit 0** 한다.
  `find -L` 또는 후행 슬래시를 쓸 것 — 이것 때문에 틀린 결론을 이슈에 게시한 적이 있다
